---
apiVersion: v1
kind: Service
metadata:
  name: &dbcluster cluster-pg17
  annotations:
    lbipam.cilium.io/ips: ${SVC_POSTGRES_IP}
    external-dns.alpha.kubernetes.io/hostname: cluster-pg17.${SECRET_DOMAIN}
spec:
  type: LoadBalancer
  ports:
    - name: postgres
      port: &port 5432
      protocol: TCP
      targetPort: *port
  selector:
    cnpg.io/cluster: *dbcluster
    cnpg.io/instanceRole: primary
